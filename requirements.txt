# Crown-path test suite (python -m tests.test_crown_path) — the only hard dependency:
pynacl                 # ed25519 signing of round records (eval/signing.py)

# AUDITOR (python -m eval.auditor, CPU, no models at L0/L1). huggingface_hub is a REAL
# requirement here, not an optional extra: the auditor's first action fetches index.json through
# HFReadSink, whose import sits outside the try, so without it `--once` dies with a traceback and
# `--follow` spins forever printing the same ImportError.
huggingface_hub        # fetch the published trail (eval/publish.py)

# EVERYTHING ELSE IS IN A SEPARATE FILE, and is a real `pip install -r`, not a comment. The chain
# SDK used to be documented here as a version range and never pinned anywhere installable, so the
# only working copy on the dev box belonged to ANOTHER PROJECT and whatever happened to be there
# was what ran:
#
#   pip install -r requirements-chain.txt    # reading commitments, setting weights, anchoring
#   pip install -r requirements-gpu.txt      # scoring real models (the rented box, L2/L3 audits)
#
# The split is deliberate: `python -m tests.test_crown_path` must stay installable on a box with no
# wallet, no GPU and no network, and folding either of those in would make it a torch-sized install.
#
# Production also needs the system package `bubblewrap` (bwrap) for the code-axis grader sandbox.
